#include<stdio.h>
int main()
{
	int a,b;
	printf("enter the a,b");
	scanf("%d%d",&a,&b);
	if(a==b)
	printf("both numbers are equal");
	else
	printf("both numbers are not equal");
	return 0;
}
